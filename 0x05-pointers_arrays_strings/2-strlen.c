#include "main.h"

/**
 * _strlen - It returns the length of a string.
 * @s: is a arguement
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != 0; i++)
	{
		_putchar('\n');
	}
}
